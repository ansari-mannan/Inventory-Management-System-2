apiVersion: v1
kind: Service
metadata:
  name: ims-backend
  labels:
    app: ims
    tier: backend
spec:
  type: ClusterIP
  selector:
    app: ims
    tier: backend
  ports:
    - name: http
      port: 80
      targetPort: 3000
