# Backend configuration (12-factor: all config via env vars)
PORT=3000
DB_HOST=localhost
DB_PORT=5432
DB_NAME=ims
DB_USER=postgres
DB_PASSWORD=postgres
DB_SSL=false
# Set to a frontend origin for local cross-origin dev, e.g. http://localhost:5173
ALLOW_ORIGIN=
